buildscript {
	dependencies {
		classpath fileTree(dir: pluginClasspathDir, include: "*.jar")
	}
}

apply plugin: "java"
apply plugin: "eclipse"

task runGradleTest

runGradleTest {
	dependsOn ":eclipse"
	
	doLast {
		// TODO assert that .project doesn't contain nature
	}
}