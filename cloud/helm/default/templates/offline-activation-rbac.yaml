{{- if and .Values.licensing.enabled .Values.licensing.offlineActivationWorkflow.enabled -}}
{{- $offlineActivationWorkflow := .Values.licensing.offlineActivationWorkflow -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
    labels:
        {{- include "liferay.labels" . | nindent 8 }}
    name: offline-activation-role-binding
    namespace: {{ include "liferay.namespace" . }}
roleRef:
    apiGroup: rbac.authorization.k8s.io
    kind: ClusterRole
    name: {{ $offlineActivationWorkflow.clusterRoleName }}
subjects:
    -   kind: ServiceAccount
        name: {{ $offlineActivationWorkflow.serviceAccountName }}
        namespace: {{ include "liferay.namespace" . }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
    labels:
        {{- include "liferay.labels" . | nindent 8 }}
    name: {{ $offlineActivationWorkflow.serviceAccountName }}
    namespace: {{ include "liferay.namespace" . }}
{{- end -}}