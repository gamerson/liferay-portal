## CX IN DOCKER

The procedure for any CX project is more or less `gw runDockerContainer`.

Make sure Liferay is running and `${liferay.home}` is valid in the workspace.

## TODO

- `clean` task should stop and clean container of CX
