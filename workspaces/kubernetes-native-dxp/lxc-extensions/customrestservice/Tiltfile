load('ext://lxc', 'lxc_extension')
lxc_extension(
  name='customrestservice',
  command='./build.sh',
  deps=['configurator/', './src', './pom.xml'],
  ignore=['./target'],
  kustomize_path='../../k8s/customrestservice'
)