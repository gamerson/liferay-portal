task repackage(type: Exec) {
	commandLine './mvnw', 'clean', 'package', 'spring-boot:repackage'
	workingDir projectDir
}

task bootJar(type: Copy) {
	dependsOn repackage
	from "${projectDir}/target"
	include "sample-etc-2-*.jar"
	into "${buildDir}/maven"
}